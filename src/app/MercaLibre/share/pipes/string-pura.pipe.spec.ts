import { StringPuraPipe } from './string-pura.pipe';

describe('StringPuraPipe', () => {
  it('create an instance', () => {
    const pipe = new StringPuraPipe();
    expect(pipe).toBeTruthy();
  });
});
