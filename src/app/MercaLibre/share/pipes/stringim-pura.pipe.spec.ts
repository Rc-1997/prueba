import { StringimPuraPipe } from './stringim-pura.pipe';

describe('StringimPuraPipe', () => {
  it('create an instance', () => {
    const pipe = new StringimPuraPipe();
    expect(pipe).toBeTruthy();
  });
});
